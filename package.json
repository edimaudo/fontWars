{
  "name": "fontwars",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "devvit build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@devvit/public-api": "latest"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
